code_review:
  disable: false  # Must be false to allow the review engine to run when called

pull_request_opened:
  summary: false      # Stops the automatic PR summary comment
  code_review: false  # Stops the automatic initial review
  help: false         # Keeps the PR thread clean