{
  "name": "openmidicontrol-commitlint",
  "version": "0.2.2",
  "private": true,
  "description": "Commitlint configuration for OpenMIDIControl",
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.3",
    "husky": "^9.0.11"
  },
  "scripts": {
    "prepare": "husky",
    "lint:commit": "commitlint --edit"
  }
}
